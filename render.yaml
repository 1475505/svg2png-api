services:
  - type: web
    name: svg2png-api
    runtime: rust
    plan: free
    buildCommand: bash build.sh
    startCommand: ./target/release/svg2png-api
    healthCheckPath: /health
    envVars:
      - key: RUST_LOG
        value: info
      - key: PORT
        value: "3000"
